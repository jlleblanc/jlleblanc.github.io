/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/jlleblanc.github.io',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
